// Vitest setup — runs before every test file.
// Registers @testing-library/jest-dom matchers (toBeInTheDocument, etc.)
// for the component tests planned in Phase 2.
import "@testing-library/jest-dom/vitest";
